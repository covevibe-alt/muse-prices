# DISABLED — Spotify blocks headless browsers from datacenter IPs.
# Keeping file for reference. Monthly listener ratios are updated via
# the Mac-based Cowork scheduled task instead.
#
# To re-enable, uncomment the workflow below.

# name: Scrape monthly listeners
# on:
#   schedule:
#     - cron: '0 6 * * *'
#   workflow_dispatch:
